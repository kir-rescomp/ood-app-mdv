batch_connect:
  template: "basic"

script:
  partition: "<%= partition %>"
  native:
    - "--cpus-per-task=<%= num_cores %>"
    - "--mem=<%= memory_gb %>G"
    - "--time=<%= walltime %>"
    - "--job-name=ood-mdv"
